services:
  caca:
    image: postgres:latest
    container_name: caca
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: lab_inspection
    restart: always
    ports:
    - 5436:5432
    volumes:
    - caca:/var/lib/postgresql/data
volumes:
  caca: null
